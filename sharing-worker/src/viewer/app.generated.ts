// Generated by npm run viewer:build. Do not edit.
export const VIEWER_APP_JS = "\"use strict\";\n(() => {\n  // ../src/shared/player/PlaybackClock.ts\n  var DRIFT_IGNORE_MS = 30;\n  var DRIFT_RESYNC_MS = 150;\n  var PlaybackClock = class {\n    constructor(master, options = {}) {\n      this.master = master;\n      this.options = options;\n      this.auxiliaries = [];\n      this.ignoreMs = options.ignoreMs ?? DRIFT_IGNORE_MS;\n      this.resyncMs = options.resyncMs ?? DRIFT_RESYNC_MS;\n    }\n    add(track) {\n      this.auxiliaries.push(track);\n      this.alignOne(track);\n      this.syncAuxiliaryPlayback(track);\n    }\n    clear() {\n      this.auxiliaries.length = 0;\n    }\n    /** Aligns auxiliaries to the master's current position without moving it. */\n    syncFromMaster() {\n      for (const track of this.auxiliaries) {\n        this.alignOne(track);\n        this.syncAuxiliaryPlayback(track);\n      }\n    }\n    async play() {\n      for (const track of this.auxiliaries) this.alignOne(track);\n      await this.master.play();\n      await Promise.allSettled(this.auxiliaries.map((track) => this.syncAuxiliaryPlayback(track)));\n    }\n    pause() {\n      this.master.pause();\n      for (const track of this.auxiliaries) track.element.pause();\n    }\n    seek(ms) {\n      this.master.currentTime = Math.max(0, ms / 1e3);\n      this.syncFromMaster();\n    }\n    setPlaybackRate(rate) {\n      this.master.playbackRate = rate;\n      for (const track of this.auxiliaries) track.element.playbackRate = rate;\n    }\n    correctDrift() {\n      let resynced = 0;\n      for (const track of this.auxiliaries) {\n        if (!this.hasStarted(track)) {\n          if (track.element.currentTime !== 0) track.element.currentTime = 0;\n          if (!track.element.paused) track.element.pause();\n          continue;\n        }\n        this.syncAuxiliaryPlayback(track);\n        const drift = track.element.currentTime - this.targetFor(track);\n        const magnitudeMs = Math.abs(drift) * 1e3;\n        if (magnitudeMs < this.ignoreMs) continue;\n        if (magnitudeMs <= this.resyncMs) {\n          this.options.onDrift?.(drift);\n          continue;\n        }\n        this.alignOne(track);\n        resynced += 1;\n      }\n      return resynced;\n    }\n    targetFor(track) {\n      return Math.max(0, this.master.currentTime - track.timelineOffsetMs / 1e3);\n    }\n    hasStarted(track) {\n      return this.master.currentTime * 1e3 >= track.timelineOffsetMs;\n    }\n    alignOne(track) {\n      track.element.currentTime = this.targetFor(track);\n      track.element.playbackRate = this.master.playbackRate;\n    }\n    syncAuxiliaryPlayback(track) {\n      if (this.master.paused || !this.hasStarted(track)) {\n        if (!track.element.paused) track.element.pause();\n        return Promise.resolve();\n      }\n      if (!track.element.paused) return Promise.resolve();\n      this.alignOne(track);\n      return track.element.play().catch(() => void 0);\n    }\n  };\n\n  // src/viewer/app.ts\n  function byId(id) {\n    const element = document.getElementById(id);\n    if (!element) throw new Error(`Missing viewer element: ${id}`);\n    return element;\n  }\n  var title = byId(\"recording-title\");\n  var recordingNav = byId(\"recordings\");\n  var viewer = byId(\"viewer\");\n  var stage = byId(\"stage\");\n  var clockLabel = byId(\"clock\");\n  var mixers = byId(\"mixers\");\n  var transcriptPanel = byId(\"transcript-panel\");\n  var transcriptList = byId(\"transcript\");\n  var topicsPanel = byId(\"topics-panel\");\n  var topicsList = byId(\"topics\");\n  var notesPanel = byId(\"notes-panel\");\n  var notesList = byId(\"notes\");\n  var unavailable = byId(\"unavailable\");\n  var state = {\n    manifest: null,\n    recording: null,\n    masterTrack: null,\n    master: null,\n    elements: /* @__PURE__ */ new Map(),\n    playbackClock: null,\n    driftTimer: null,\n    activeTranscript: -1\n  };\n  void boot().catch(showUnavailable);\n  async function boot() {\n    const response = await fetch(\"/viewer/manifest\", { cache: \"no-store\" });\n    if (!response.ok) return showUnavailable();\n    const manifest = await response.json();\n    if (!manifest || !Array.isArray(manifest.recordings) || manifest.recordings.length === 0) {\n      return showUnavailable();\n    }\n    state.manifest = manifest;\n    renderRecordingNav(manifest.recordings);\n    await loadRecording(manifest.recordings[0]);\n  }\n  function renderRecordingNav(recordings) {\n    recordingNav.replaceChildren();\n    recordingNav.hidden = recordings.length < 2;\n    recordings.forEach((recording, index) => {\n      const button = document.createElement(\"button\");\n      button.type = \"button\";\n      button.textContent = recording.title || `Recording ${index + 1}`;\n      button.dataset.recordingId = recording.id;\n      button.addEventListener(\"click\", () => {\n        void loadRecording(recording);\n      });\n      recordingNav.append(button);\n    });\n  }\n  async function loadRecording(recording) {\n    stopPlayback();\n    state.recording = recording;\n    title.textContent = recording.title || \"Shared recording\";\n    viewer.hidden = false;\n    unavailable.hidden = true;\n    for (const button of recordingNav.querySelectorAll(\"button\")) {\n      button.setAttribute(\"aria-current\", String(button.dataset.recordingId === recording.id));\n    }\n    renderTopics(recording.topics ?? []);\n    renderNotes(recording.notations ?? []);\n    renderTranscript(recording.transcript?.segments ?? []);\n    const tracks = recording.tracks ?? [];\n    const masterTrack = tracks.find((track) => track.stream === \"tab\") ?? tracks[0];\n    if (!masterTrack) return showUnavailable();\n    state.masterTrack = masterTrack;\n    const master = mediaElement(masterTrack, true);\n    master.classList.add(\"master\");\n    master.controls = true;\n    master.preload = \"metadata\";\n    master.src = masterTrack.mediaEndpoint;\n    stage.append(master);\n    state.master = master;\n    state.elements.set(masterTrack.id, master);\n    state.playbackClock = new PlaybackClock(master);\n    bindMaster(master);\n    for (const track of tracks) {\n      if (track.id === masterTrack.id) continue;\n      const element = mediaElement(track, false);\n      element.preload = \"metadata\";\n      element.src = track.mediaEndpoint;\n      if (track.stream === \"self-video\") {\n        element.classList.add(\"aux-video\");\n        element.muted = true;\n        if (element instanceof HTMLVideoElement) element.playsInline = true;\n        stage.append(element);\n      } else {\n        element.hidden = true;\n        stage.append(element);\n      }\n      state.elements.set(track.id, element);\n      state.playbackClock.add({\n        element,\n        timelineOffsetMs: track.captureStartOffsetMs - masterTrack.captureStartOffsetMs\n      });\n    }\n    renderMixers(tracks);\n  }\n  function mediaElement(track, master) {\n    const element = document.createElement(track.stream === \"mic\" ? \"audio\" : \"video\");\n    element.dataset.trackId = track.id;\n    if (master && element instanceof HTMLVideoElement) element.playsInline = true;\n    element.addEventListener(\"error\", () => {\n      if (element === state.master) showUnavailable();\n    });\n    return element;\n  }\n  function bindMaster(master) {\n    master.addEventListener(\"play\", () => {\n      state.playbackClock?.syncFromMaster();\n      startDriftWatch();\n    });\n    master.addEventListener(\"pause\", () => {\n      state.playbackClock?.syncFromMaster();\n      stopDriftWatch();\n    });\n    master.addEventListener(\"seeking\", () => state.playbackClock?.syncFromMaster());\n    master.addEventListener(\"seeked\", () => state.playbackClock?.syncFromMaster());\n    master.addEventListener(\"ratechange\", () => state.playbackClock?.setPlaybackRate(master.playbackRate));\n    master.addEventListener(\"loadedmetadata\", updatePosition);\n    master.addEventListener(\"durationchange\", updatePosition);\n    master.addEventListener(\"timeupdate\", () => {\n      updatePosition();\n      syncTranscript(master.currentTime * 1e3);\n    });\n  }\n  function startDriftWatch() {\n    if (state.driftTimer) return;\n    state.driftTimer = setInterval(() => state.playbackClock?.correctDrift(), 400);\n  }\n  function stopDriftWatch() {\n    if (!state.driftTimer) return;\n    clearInterval(state.driftTimer);\n    state.driftTimer = null;\n  }\n  function stopPlayback() {\n    stopDriftWatch();\n    state.playbackClock?.pause();\n    stage.replaceChildren();\n    mixers.replaceChildren();\n    state.master = null;\n    state.masterTrack = null;\n    state.elements.clear();\n    state.playbackClock = null;\n    state.activeTranscript = -1;\n  }\n  function renderMixers(tracks) {\n    mixers.replaceChildren();\n    for (const track of tracks) {\n      if (track.stream === \"self-video\") continue;\n      const element = state.elements.get(track.id);\n      if (!element) continue;\n      const row = document.createElement(\"label\");\n      row.className = \"mixer\";\n      const name = document.createElement(\"span\");\n      name.textContent = track.stream === \"mic\" ? \"Mic\" : track.stream === \"tab\" ? \"Meeting\" : track.stream;\n      const slider = document.createElement(\"input\");\n      slider.type = \"range\";\n      slider.min = \"0\";\n      slider.max = \"1\";\n      slider.step = \"0.01\";\n      slider.value = \"1\";\n      slider.setAttribute(\"aria-label\", `${name.textContent} volume`);\n      const output = document.createElement(\"output\");\n      output.textContent = \"100%\";\n      slider.addEventListener(\"input\", () => {\n        const level = Number(slider.value);\n        element.volume = level;\n        element.muted = level === 0;\n        output.textContent = `${Math.round(level * 100)}%`;\n      });\n      row.append(name, slider, output);\n      mixers.append(row);\n    }\n  }\n  function renderTranscript(segments) {\n    transcriptList.replaceChildren();\n    transcriptPanel.hidden = segments.length === 0;\n    segments.forEach((segment, index) => {\n      const row = document.createElement(\"button\");\n      row.type = \"button\";\n      row.className = \"transcript-line\";\n      row.dataset.index = String(index);\n      const meta = document.createElement(\"strong\");\n      meta.textContent = `${formatTime(segment.tStartMs / 1e3)}${segment.speaker ? ` \\xB7 ${segment.speaker}` : \"\"}`;\n      const text = document.createElement(\"span\");\n      text.textContent = segment.text;\n      row.append(meta, text);\n      row.addEventListener(\"click\", () => seekMs(segment.tStartMs));\n      transcriptList.append(row);\n    });\n  }\n  function syncTranscript(positionMs) {\n    const segments = state.recording?.transcript?.segments ?? [];\n    let next = -1;\n    for (let index = 0; index < segments.length; index += 1) {\n      if (positionMs >= segments[index].tStartMs && positionMs <= segments[index].tEndMs) {\n        next = index;\n        break;\n      }\n    }\n    if (next === state.activeTranscript) return;\n    transcriptList.querySelector(\".active\")?.classList.remove(\"active\");\n    if (next >= 0) {\n      const current = transcriptList.querySelector(`[data-index=\"${next}\"]`);\n      if (current) {\n        current.classList.add(\"active\");\n        current.scrollIntoView({ block: \"nearest\", behavior: \"smooth\" });\n      }\n    }\n    state.activeTranscript = next;\n  }\n  function renderTopics(topics) {\n    topicsList.replaceChildren();\n    topicsPanel.hidden = topics.length === 0;\n    for (const topic of topics) {\n      const button = document.createElement(\"button\");\n      button.type = \"button\";\n      button.className = \"topic\";\n      const label = document.createElement(\"span\");\n      label.textContent = topic.keywords.slice(0, 3).join(\" \\xB7 \") || \"Topic\";\n      const meta = document.createElement(\"small\");\n      meta.textContent = formatDuration(topic.totalMs);\n      button.append(label, meta);\n      button.addEventListener(\"click\", () => seekMs(topic.spans[0]?.tStartMs ?? 0));\n      topicsList.append(button);\n    }\n  }\n  function renderNotes(notes) {\n    notesList.replaceChildren();\n    notesPanel.hidden = notes.length === 0;\n    for (const note of notes) {\n      const button = document.createElement(\"button\");\n      button.type = \"button\";\n      button.className = \"note\";\n      const label = document.createElement(\"span\");\n      label.textContent = note.text || \"Note\";\n      const meta = document.createElement(\"small\");\n      meta.textContent = formatTime(note.tStartMs / 1e3);\n      button.append(label, meta);\n      button.addEventListener(\"click\", () => seekMs(note.tStartMs));\n      notesList.append(button);\n    }\n  }\n  function seekMs(ms) {\n    if (!state.master) return;\n    if (state.playbackClock) state.playbackClock.seek(ms);\n    else state.master.currentTime = Math.max(0, ms / 1e3);\n  }\n  function updatePosition() {\n    if (!state.master) return;\n    const duration = Number.isFinite(state.master.duration) ? state.master.duration : state.recording?.durationMs != null ? state.recording.durationMs / 1e3 : 0;\n    clockLabel.textContent = `${formatTime(state.master.currentTime)} / ${formatTime(duration)}`;\n  }\n  function formatTime(seconds) {\n    const value = Number.isFinite(seconds) && seconds > 0 ? Math.floor(seconds) : 0;\n    const minutes = Math.floor(value / 60);\n    const rest = value % 60;\n    return `${minutes}:${String(rest).padStart(2, \"0\")}`;\n  }\n  function formatDuration(ms) {\n    const minutes = Math.max(1, Math.round(ms / 6e4));\n    return `${minutes} min`;\n  }\n  function showUnavailable() {\n    stopPlayback();\n    viewer.hidden = true;\n    unavailable.hidden = false;\n    title.textContent = \"Shared recording\";\n  }\n})();\n";
